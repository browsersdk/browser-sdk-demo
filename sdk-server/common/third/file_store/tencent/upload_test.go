package tencent

import (
	"testing"
)

func TestUploadFile(t *testing.T) {
	// oss := NewCOSHandler(
	// 	TENCENT_REGION,
	// 	TENCENT_BUCKET,
	// 	CDN_DOMAIN,
	// 	TENCENT_SECRET_ID,
	// 	TENCENT_SECRET_KEY,
	// )

	// oss.UploadFile()
	// url, err := UploadFile("na-siliconvalley", "biz-1328829295", "test/README.md", "/root/works/mates/mate_ai/README.md")
	// if err != nil {
	// 	log.Fatalf("Error uploading file: %v", err)
	// }
	// fmt.Printf("Uploaded File URL: %s\n", url)
}
